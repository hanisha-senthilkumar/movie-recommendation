{
  "name": "cinematch-root",
  "version": "1.0.0",
  "description": "CineMatch — AI-Powered Movie Recommendation Platform Root Orchestrator",
  "scripts": {
    "start": "npx concurrently \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:ml\"",
    "dev": "npm start",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:ml": "cd ml-service && python main.py",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
